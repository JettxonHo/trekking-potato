# TP-P0-001 完成记录

- Task ID: `TP-P0-001`
- Title: 修复 Open-Meteo 风速单位与系统 `m/s` 契约
- Final status: `DONE`
- Controller decision: `VERIFIED_COMPLETE`
- Governance version: `TP-GOV-1.0.0`
- Plan version: `1.0.0`
- Priority: `P0`
- Completed at: `2026-08-04T03:18:18+08:00`
- Merged PR: `#3`
- Source branch: `fix/tp-p0-001-wind-unit`
- Reviewed head SHA: `c9dd85b5dc17c8b82424015764ce09b71f57bd7e`
- Squash commit SHA: `2be4166e18af080554bab4722fe40dfdb70ebeac`
- Controller-owned: `true`

## 调查阶段

调查阶段已经单独归档至：

- `docs/tasks/completed/TP-P0-001-investigation.md`

调查确认：

- Open-Meteo 未声明风速单位时默认返回 `km/h`；
- 原系统将该数值直接保存为 `windMs`；
- Prompt 和前端将其解释为 `m/s`；
- 单位契约在 API 请求边界断裂。

## 最终实施

采用方案 A：

1. 请求参数显式增加 `wind_speed_unit=ms`；
2. 严格校验 `daily_units.wind_speed_10m_max === "m/s"`；
3. 缺失或错误单位返回 `weather_data_invalid`；
4. `windMs` 始终保存米每秒数值；
5. 天气对象返回 `windUnit: "m/s"`；
6. Prompt 和前端现有 `m/s` 文字保持不变；
7. 新增离线天气契约测试；
8. 新增 Prompt 单位回归断言。

## 最终验证

- `node scripts/weather-contract-test.js`：PASS 6 / FAIL 0
- `node scripts/unit-test.js`：PASS 28 / FAIL 0
- `node scripts/e2e-local.js`：保持既有环境失败，缺少 `wx-server-sdk`
- 产品实现已进入 `main`
- 未修改 Prompt 实现
- 未修改前端
- 未修改编排代码
- 未添加依赖
- 未修改 lock 文件
- 旧本地缓存最长约 30 分钟，自然过期，无需迁移历史数据

## 合并时同步指纹

```text
MASTER_PLAN SHA-256:
9247e37e101ab04492a28e370f60d1a3d6eae58fbdc511323b435fb76e3d55de

ACTIVE_TASK SHA-256:
2fdecb6ea4bc5ca237b38f2db23ca96a3a037eb97ba762cf42590b0f63a1132c

```

后续任务
下一项 P0 正确性任务为：

* `TP-P0-002`：调查天气窗口是否从用户选择的出发日期开始，并严格对应行程天数。

该任务必须通过新的 `ACTIVE_TASK.md` 单独授权。
